# AresOS — Дорожная карта
# AresOS — Дорожная карта


**Цель проекта:** 64-битная ОС «голого железа» (x86-64) на C + NASM с собственным
**Цель проекта:** 64-битная ОС «голого железа» (x86-64) на C + NASM с собственным
UEFI-загрузчиком, которая в финале запускает Windows-приложения формата PE (.exe)
UEFI-загрузчиком, которая в финале запускает Windows-приложения формата PE (.exe)
через собственный слой совместимости WinAPI (подход, как у Wine/ReactOS, но «снизу»).
через собственный слой совместимости WinAPI (подход, как у Wine/ReactOS, но «снизу»).


**Честная оценка:** полноценный запуск любых Windows-программ — это уровень Wine
**Честная оценка:** полноценный запуск любых Windows-программ — это уровень Wine
(30+ лет разработки командой). Наш реалистичный ориентир: сначала **консольные
(30+ лет разработки командой). Наш реалистичный ориентир: сначала **консольные
.exe** (hello world, скомпилированный MinGW), затем последовательное расширение
.exe** (hello world, скомпилированный MinGW), затем последовательное расширение
API. Каждый этап ниже — рабочая, проверяемая версия ОС.
API. Каждый этап ниже — рабочая, проверяемая версия ОС.


---
---


## M0. Инфраструктура ✅ (текущий этап)
## M0. Инфраструктура ✅ (текущий этап)


- [x] Репозиторий, лицензия, структура каталогов
- [x] Репозиторий, лицензия, структура каталогов
- [ ] Установлено окружение по [docs/SETUP.md](SETUP.md)
- [ ] Установлено окружение по [docs/SETUP.md](SETUP.md)
- [ ] Все 6 проверочных команд из SETUP.md работают
- [ ] Все 6 проверочных команд из SETUP.md работают


**Definition of Done:** `x86_64-elf-gcc --version` и `qemu-system-x86_64 --version` отвечают.
**Definition of Done:** `x86_64-elf-gcc --version` и `qemu-system-x86_64 --version` отвечают.

---

## M1. UEFI-загрузчик + ядро-пустышка ✅ СДЕЛАНО

- UEFI-приложение (`BOOTX64.EFI`) на C с **собственными определениями UEFI** (`boot/uefi.h`,
  без gnu-efi; все вызовы через `ms_abi`):
  - [x] инициализация консоли (ConOut) + зеркалирование в COM1;
  - [x] чтение `KERNEL.ELF` с ESP (FAT12, генерирует наш `tools/mkesp.py`);
  - [x] парсинг ELF64, загрузка PT_LOAD-сегментов по физическим адресам;
  - [x] `GetMemoryMap` + видеорежим через GOP (1024x768 или текущий);
  - [x] `ExitBootServices` (с retry-циклом) и прыжок в ядро с `bootinfo_t*`.
- [x] Ядро: вход на GAS (`kernel/arch/x86_64/entry.S`) → `kmain()` → `hlt`.
- [x] `Makefile`: сборка, образ, запуск QEMU+OVMF, цели run/debug/gdb/verify.
- [x] Linker script `kernel/linker.ld`.
- [x] Свой конвертер `tools/elf2efi.py`: static-PIE ELF → PE32+ EFI app (учебный PE-парсер пригодится на M8).

**DoD:** `make run` → serial-лог загрузчика и баннер ядра. (Собрано и проверено
статически; финальный тест — на машине с QEMU, см. docs/SETUP.md, шаг 4.)

## M2. Диагностика ядра ✅ СДЕЛАНО

- [x] Serial port COM1 (0x3F8), 115200 8N1 (`-serial stdio`).
- [x] `kprintf()` — свой printf (`%d %u %x %X %p %s %c`, l/ll, ширина, '-', '0').
- [x] Framebuffer-консоль 8x8 (font8x8, public domain) со скроллом.
- [x] GDT (null/kcode/kdata), **IDT** на 32 исключения со стабами на GAS,
      дамп всех регистров + CR2 для #PF + `kpanic`.

**DoD:** искусственный `#PF` печатает причину и регистры в serial и на экран.

## M2.5 (бонус). Прототип рабочего стола ✅

- [x] gfx-библиотека: fill_rect, градиент, рамки, текст (font8x8)
- [x] legacy PIC 8259: ремап векторов 0x20/0x28, маски, EOI
- [x] драйвер мыши PS/2 через IRQ12 (контроллер 8042), пакеты 3 байта
- [x] десктоп: обои-градиент, верхняя панель, док, окно с перетаскиванием
      за заголовок, XOR-курсор
- [x] IDT расширен до векторов 32..47 (IRQ0..15), sti включён
- [x] **v0.5.0, современный UI «midnight aurora»**: кэш строк обоев (быстрый
      redraw), плавающий док со значками, окна со z-порядком/raise/кнопкой
      закрытия, скругления (gfx_fill_round_rect), двухцветный курсор через
      save/restore, часы в панели, диспетчер задач с барами загрузки CPU,
      приложение «Логи» — весь вывод kprintf собирается в кольцевой буфер
      logbuf (цветной терминал с прокруткой ↑/↓/PgUp/PgDn)
- [x] v0.5.0: клавиатурная мышь (Ctrl+стрелки) — страховка для ВМ
- [x] v0.5.2: приложения запускаются значками (кнопки панели + док, клик),
      закрытие окон красной × (Esc дублирует); кириллический шрифт 8x8
      (66 глифов, свой генератор tools/cyrfont_gen.py) + UTF-8 декодер —
      консоль ядра и весь desktop-UI по-русски
- [x] v0.5.1 (hotfix по фото пользователя): LAPIC-калибровка — двойной
      замер ±5%, иначе честный откат на PIT (на NEM одиночный замер врал →
      таймер летел на мегагерцы, часы и рефреши UI «варпились»); детектор
      кадра IRQ принимает код TESTPE.EXE (0x400000000) — не вешает систему;
      зона logbuf/fb защищена от повторного входа из IRQ (мусор 'P''Q'
      на экране исчез); перетаскивание окон — частичный ремонт области
      (union rect), мерцание убрано; стеки потоков 16→32 КиБ

Полноценный оконный менеджер/приложения (user-mode) — после M6 (ФС) и M7
(userspace); текущие окна — ядерные «вьюхи», их формат к M7 перепишется.

## M3. Управление памятью ✅ СДЕЛАНО (v0.3.0; higher-half — отдельным шагом M3.1)

- [x] **PMM**: битовая карта страниц 4 КиБ по UEFI-карте памяти (`kernel/mm/pmm.c`),
      alloc/free, статистика, дымовой тест в kmain.
- [x] **VMM** (`kernel/mm/vmm.c`): свои таблицы страниц, CR3 переключается ещё до кучи:
  - identity-маппинг 0..4 ГиБ 2-МиБ страницами;
  - **HHDM** (вся физпамять продублирована на `0xFFFF800000000000`);
  - **null-page guard**: страница 0 не замаплена — разыменование NULL = #PF с именем виновника;
  - зона ядра постранично: `.text` RX, `.rodata` R, данные/стек RW+NX;
  - `EFER.NXE`, `CR0.WP` (RO защищает даже ring-0), `vmm_map_4k/protect/get_phys/unmap`.
- [x] **Kernel heap** (`kernel/mm/heap.c`): `kmalloc/kfree`, K&R-аллокатор на арене
      2 МиБ поверх VMM (split + coalesce).
- [x] **M8-пробник**: `tools/mkpe.py` собирает настоящий **PE32+ TESTPE.EXE**, а
      `kernel/pe.c` разбирает его **строго по спецификации PE/COFF** (MZ/PE/COFF/
      Optional, секции, каталоги импортов/релоков), мапит страницы (RX!) и вызывает
      точку входа. Приложение рисует шахматный квадрат на десктопе и возвращает
      `0xA2E5`. Десктоп печатает результат.
- [ ] (M3.1) Переезд ядра в higher half `0xFFFFFFFF80000000` (+ перелинковка, boot-half трюк).

**DoD:** стресс-тест аллокатора (миллион случайных alloc/free) без утечек и паник —
реализован в `heap_stress_test()`, печатает `STRESS OK ... leaks=0`.

## M4. Прерывания и время ✅ СДЕЛАНО (v0.4.0)

- ACPI: RSDP берём из EFI ConfigurationTable (GUID ACPI 2.0/1.0; legacy-скан
  0xE0000 как запасной путь), разбор XSDT/RSDT → MADT → LAPIC/IOAPIC адреса,
  BSP APIC ID, GSI-overrides. `kernel/drivers/acpi.c`.
- LAPIC-таймер 100 Гц: калибровка по PIT каналу 2 (one-shot ~50 мс),
  periodic-режим, вектор 0x40. `kernel/drivers/lapic.c`.
- IOAPIC: клавиатура (IRQ1→vec 33) и мышь (IRQ12→vec 44) маршрутизируются
  на BSP; старая модель «PIC 8259 + PIT ch0 100 Гц» оставлена как автоматический
  fallback, если ACPI/MADT нет или калибровка не сошлась.
- Клавиатура PS/2: IRQ-обработчик, scancode set 1 → ASCII (Shift/CapsLock),
  F1..F10, стрелки (E0-префикс), кольцевой буфер на 64 события. `kernel/drivers/keyboard.c`.
- Отложено: MSI, x2APIC, HPET/TSC-калибровка, `rdtsc`-профилирование.

**DoD выполнен:** ядро реагирует на клавиши (последняя клавиша рисуется в док),
аптайм по системному тику 100 Гц обновляется каждую секунду.

## M5. Многозадачность ✅ СДЕЛАНО (v0.4.0)

- Ядровые потоки (ring-0): у каждого свой стек 16 КиБ (kmalloc),
  старт — через фейковый IRQ-кадр + `iretq` в `proc_trampoline`. `kernel/proc.c`.
- Планировщик round-robin: переключение ТОЛЬКО из тика таймера — подмена
  указателя стека перед `pop/iretq` в `irq_common` (все кадры одной формы).
  Квант 4 тика (40 мс), вытеснение, сон по `proc_sleep(ms)` с пробуждением
  по wake_tick, лимит 32 процесса.
- Потоки: `idle` (kmain), `desktop` (обычный), фоновые демоны `heartbeat`
  (мигает огоньком в доке) и `sysmon` (пульс в serial).
- **Диспетчер задач на десктопе (F2)**: таблица id/имя/состояние/тики CPU/тип
  (normal/background), обновление 4 раза в секунду, подсветка текущего потока.
- Проверено host-симуляцией логики планировщика (честный RR, квант, сон/будка,
  лимит) — тот же приём, что поймал 2 бага кучи в v0.3.2.
- Отложено (M5.1+): приоритеты, spinlock/mutex, SMP, FXSAVE (мы пока без FPU-
  контекста, `-mgeneral-regs-only`), user-mode задачи и TSS.

**DoD выполнен:** heartbeat (фон) и desktop (обычный) крутятся параллельно —
огонёк мигает, мышь/окна отзываются, диспетчер показывает живые счётчики тиков.

## M6. Диск и файловая система
_3–5 недель_

- Драйвер диска: AHCI (SATA) — стандартный путь; (вариант проще на старте: initrd-образ, загруженный загрузчиком в RAM).
- **VFS** — виртуальная ФС: единый интерфейс open/read/write/readdir.
- **FAT32** (чтение+запись) поверх VFS. NTFS — только чтение, намного позже.

**DoD:** ядро монтирует диск, `cat /readme.txt` в нашей консоли читает файл с реального образа.

## M7. Пользовательский режим
_4–8 недель_

- Ring 3: TSS, переключение в user mode.
- Syscall-интерфейс: инструкция `syscall/sysret` (не int 0x80 — быстрее).
- Загрузчик **ELF**-программ AresOS, мини-libc, shell.
- Изоляция адресных пространств процессов (каждому — свои таблицы страниц).

**DoD:** shell запускает `hello.elf` как отдельный процесс; краш процесса не роняет ядро.

## M8. Подсистема PE / WinAPI — «Долгая дорога»
_Постоянный этап, начинается когда стабилен M7_

1. **PE loader** (по Microsoft PE/COFF Specification):
   парсинг заголовков DOS/NT, маппинг секций (.text, .data, .rdata) по виртуальным
   адресам, ImageBase, **релокации** (.reloc), таблица импортов (IAT).
2. **kernel32/ntdll-совместимость** — наша реализация:
   - минимум для консольного hello.exe (MinGW): `GetStdHandle`, `WriteConsoleA`,
     `ExitProcess`, `VirtualAlloc/VirtualFree`, `HeapAlloc`, `GetLastError`...;
   - TEB/PEB-структуры, SEH позже.
3. Тестовый стенд: `x86_64-w64-mingw32-gcc hello.c -o hello.exe` → AresOS его запускает.
4. Расширение API-сета → файлы, потоки (CreateThread), DLL → user32/gdi32-стыбы →
   собственная графическая подсистема.

**DoD этапа 1–3:** `hello.exe`, собранный под Windows x64, печатает текст в консоли AresOS.

⚠️ **Лицензии:** AresOS — Apache-2.0. Код Wine (LGPL) и ReactOS (GPL) **копировать
нельзя** — изучаем идеи и пишем своё по документации Microsoft Learn и PE/COFF spec.

---

## Библиотека разработчика (что читать на каждом этапе)

| Этап | Документ |
|---|---|
| Все | **OSDev Wiki** (wiki.osdev.org) — главный справочник |
| M1 | **UEFI Specification** (uefi.org), разделы Boot Services, GOP |
| M2, M3, M4 | **Intel SDM, том 3** (intel.com): главы про GDT/IDT (6), Paging (4/5), APIC (10) |
| M7, M8 | Intel SDM vol.2 (инструкции syscall), System V AMD64 ABI |
| M8 | **Microsoft PE and COFF Specification**, Microsoft Learn (WinAPI) |
