# Build artifacts
build/
# NB: dist/ НЕ игнорируется — туда осознанно коммитим zip с готовым образом
# для скачивания без сборки (пока CI недоступен). Размер контролируем.
*.o
*.d
*.elf
*.efi
*.bin
*.img
*.iso
*.so

# Image / floppy dirs created by mtools scripts
esp/

# Toolchain (собирается локально, не коммитим)
cross/
opt/

# Editors
.vscode/
*.swp
__pycache__/
*.pyc
